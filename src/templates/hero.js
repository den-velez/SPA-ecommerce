const Hero = () => {
  return ` 
  <section class="hero">
        <figure class="hero-imgContainer">
          <img src="./src/images/hero.svg" alt="big photo" />
        </figure>
      </section>
      `;
};

export default Hero;
